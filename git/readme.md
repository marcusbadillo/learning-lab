# git

## git submodules

add a sudmodule

```bash
git submodule add https://github.com/yourname/other-repo.git examples/other-repo
git commit -m "Add other-repo as submodule under examples/"
```

update a sudmodule

```bash
git submodule update --remote --merge
```
